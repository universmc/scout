[RUN][Devops]phase2:initialisation de la base de donnée + emoji 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

groq -R `user,content:'${Ressource}+CDN BOOTSTRAP'` 

gro